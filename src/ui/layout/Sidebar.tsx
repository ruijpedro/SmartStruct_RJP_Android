import { modules } from '../../core/constants/modules';
export function Sidebar({active,onSelect}:{active:string;onSelect:(id:string)=>void}){ const groups=[...new Set(modules.map(m=>m.group))]; return <aside className="sidebar"><div className="brand">SmartStruct_RJP</div><div className="muted">Academic & IP Edition</div>{groups.map(g=><div className="navgroup" key={g}><div className="navtitle">{g}</div>{modules.filter(m=>m.group===g).map(m=><button key={m.id} className={'navbtn '+(active===m.id?'active':'')} onClick={()=>onSelect(m.id)}>{m.icon} {m.label}</button>)}</div>)}</aside>}
